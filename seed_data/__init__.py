"""Externalised seed content (module definitions + extra quiz questions).

Each submodule is imported explicitly by seed.py; this __init__ stays empty so
importing one submodule does not require its siblings to exist yet.
"""
